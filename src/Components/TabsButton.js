import React from "react"

export function TabsButton({ children }) {
    return (
        <button className="TabsButton icon_button cta" type="submit">
            {children}
        </button>
    )
}
